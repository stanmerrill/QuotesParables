@model IEnumerable<QuotesParables.Models.QuoteType>

@{
    ViewBag.Title = "Index";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QuoteTypeDescription)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QuoteTypeDescription)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.QuoteTypeId }) |
                @Html.ActionLink("Details", "Details", new { id=item.QuoteTypeId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.QuoteTypeId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
